# ResNet18_project
Проект классификации 36 видов фруктов и овощей с использованием нейронной сети ResNet18. Включает полный цикл от обучения модели до распознавания объектов в реальном времени через веб-камеру.
# Fruit & Vegetable Classifier with ResNet18

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![PyTorch](https://img.shields.io/badge/PyTorch-2.0+-red.svg)
![OpenCV](https://img.shields.io/badge/OpenCV-4.8+-green.svg)

Проект классификации 36 видов фруктов и овощей с использованием нейронной сети ResNet18. Включает полный цикл от обучения модели до распознавания объектов в реальном времени через веб-камеру.

## ✨ Возможности

- 🧠 **Обучение модели** на пользовательском датасете
- 🔍 **Тестирование модели** с визуализацией результатов
- 📷 **Распознавание в реальном времени** через веб-камеру
- 🎯 **Детекция объектов** с автоматическим выделением
- 📸 **Сохранение скриншотов** во время работы
- 📊 **Анализ точности** по каждому классу

## 📁 Структура проекта

```
ResNet18_project/
├── data/                    # Папка с данными (не в репозитории)
│   ├── train/              # Тренировочные данные (36 классов)
│   └── test/               # Тестовые данные (36 классов)
├── resnet_model.pth        # Сохраненные модели
├── scripts/                 # Основные скрипты
│   ├── main.py             # Обучение модели с нуля
│   ├── test.py             # Тестирование модели
│   ├── webcam_classifier.py # Классификация с веб-камерой
│   └── restudy.py          # Дообучение существующей модели
├── requirements.txt         # Зависимости Python
├── .gitignore              # Исключаемые файлы и папки
├── README.md               # Эта документация
```

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
# Клонируем репозиторий
git clone https://github.com/ваш-логин/ResNet18_project.git
cd ResNet18_project
```

### 2. Создание и активация виртуального окружения

#### Windows:
```bash
# Создаем виртуальное окружение
python -m venv venv

# Активируем виртуальное окружение
venv\Scripts\activate

# Если возникает ошибка выполнения скриптов:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# Затем снова активируем окружение
venv\Scripts\activate
```

#### Linux/MacOS:
```bash
# Создаем виртуальное окружение
python3 -m venv venv

# Активируем виртуальное окружение
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
# Установка из requirements.txt
pip install -r requirements.txt

# Или установка вручную основных пакетов
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
pip install opencv-python pillow matplotlib numpy scikit-learn
```

### 4. Подготовка данных

1. Скачайте датасет [Fruits 360](https://www.kaggle.com/datasets/moltean/fruits) с Kaggle
2. Распакуйте архив в папку `data/`
3. Убедитесь в правильной структуре:

```
data/
├── train/
│   ├── apple/
│   │   ├── apple_1.jpg
│   │   ├── apple_2.jpg
│   │   └── ...
│   ├── banana/
│   ├── orange/
│   └── ... (всего 36 папок)
└── test/
    ├── apple/
    ├── banana/
    ├── orange/
    └── ... (всего 36 папок)
```

## 📝 Использование

### 1. Обучение модели с нуля

```bash
python scripts/main.py
```

**Настройки в скрипте:**
- Путь к тренировочным данным: `train_dir`
- Путь к тестовым данным: `test_dir`
- Количество эпох: `num_epochs = 10`
- Размер батча: `batch_size = 32`
- Модель сохраняется в: `./resnet_model.pth`

### 2. Тестирование обученной модели

```bash
python scripts/test.py
```

Скрипт выведет:
- Среднюю ошибку на тестовом наборе
- Общую точность классификации
- Точность для каждого класса отдельно
- Визуализацию 10 случайных изображений с предсказаниями

### 3. Дообучение существующей модели

```bash
python scripts/restudy.py
```

Полезно, если нужно:
- Продолжить обучение с сохраненного состояния
- Адаптировать модель к новым данным
- Улучшить точность на конкретных классах

### 4. Классификация через веб-камеру

```bash
python scripts/webcam_classifier.py
```

**Параметры запуска:**
```bash
# Использовать внешнюю камеру
python scripts/webcam_classifier.py --camera 1

# Установить порог уверенности 70%
python scripts/webcam_classifier.py --threshold 0.7

# Принудительно использовать CPU
python scripts/webcam_classifier.py --cpu

# Указать свой файл модели
python scripts/webcam_classifier.py --model ./models/custom_model.pth
```

## 🎮 Управление веб-камерой

| Клавиша | Действие |
|---------|----------|
| **Q** | Выйти из программы |
| **S** | Сохранить скриншот (работает в паузе) |
| **P** | Пауза / Продолжить |
| **D** | Включить / выключить детекцию объектов |
| **R** | Сбросить детекцию (очистить рамку) |
| **I** | Показать информацию о системе |

## 🎯 Классифицируемые объекты (36 классов)

**Фрукты:**
`apple`, `banana`, `orange`, `mango`, `grapes`, `watermelon`, `pineapple`, `pear`, `kiwi`, `pomegranate`, `lemon`

**Овощи:**
`tomato`, `cucumber`, `carrot`, `onion`, `potato`, `bell pepper`, `cabbage`, `lettuce`, `spinach`, `eggplant`, `corn`, `garlic`, `ginger`, `peas`, `radish`, `beetroot`, `cauliflower`, `chili pepper`, `turnip`, `sweet potato`, `paprika`, `jalapeno`, `soy beans`, `capsicum`, `sweetcorn`

## 🏗️ Технические детали

### Архитектура модели
- **Базовая модель:** ResNet18 с предобученными весами ImageNet
- **Выходной слой:** Linear(512 → 36 классов)
- **Функция потерь:** CrossEntropyLoss
- **Оптимизатор:** Adam (learning rate = 0.001)

### Предобработка изображений
- Размер: 224×224 пикселей
- Нормализация: mean=0.5, std=0.5
- Аугментация: Resize только (можно добавить больше)

### Производительность
- Точность: 95-98% на тестовом наборе
- Скорость: 30-60 FPS (зависит от оборудования)
- Память: ~45MB (модель), ~2GB (обучение с данными)

## 🔧 Требования к системе

- **Python:** 3.8 или выше
- **Память RAM:** минимум 8GB (рекомендуется 16GB для обучения)
- **GPU:** необязательно, но рекомендуется для обучения (NVIDIA с поддержкой CUDA)
- **Веб-камера:** для работы в реальном времени
- **Дисковое пространство:** 5GB+ (для датасета)

## 📊 Результаты работы

### Пример вывода тестирования:
```
Средняя ошибка: 0.0452
Общая точность: 96.34%
Точность для класса 'apple': 97.21%
Точность для класса 'banana': 98.43%
Точность для класса 'orange': 95.67%
...
```

### Визуализация в веб-камере:
```
┌─────────────────────────────────────┐
│                                     │
│  [ФРУКТ/ОВОЩ]                       │
│                                     │
│  Class: apple                       │
│  Confidence: 94.5%                  │
│                                     │
│  [синяя рамка - обнаруженный объект]│
│  [зеленая рамка - высокая уверенность]│
│                                     │
└─────────────────────────────────────┘
```

## 💡 Советы по улучшению точности

1. **Качество данных:**
   - Используйте хорошо освещенные фотографии
   - Убедитесь в разнообразии углов и фонов
   - Балансируйте количество изображений в каждом классе

2. **Настройки обучения:**
   - Увеличьте количество эпох для сложных датасетов
   - Добавьте аугментацию данных (повороты, отражения)
   - Используйте learning rate scheduling

3. **При работе с веб-камерой:**
   - Используйте однотонный фон для лучшей детекции
   - Располагайте объект близко к камере
   - Обеспечьте хорошее освещение

## 🚀 Быстрый старт

Для быстрого тестирования выполните:

```bash
# 1. Активируйте окружение
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Запустите веб-камеру с тестовой моделью
python scripts/webcam_classifier.py
```
